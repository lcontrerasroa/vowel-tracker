import React from 'react'
import ReactDOM from 'react-dom/client'
import VowelTracker from './VowelTracker.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VowelTracker />
  </React.StrictMode>
)
